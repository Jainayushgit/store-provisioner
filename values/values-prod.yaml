namespace: platform

ingress:
  enabled: true
  className: traefik
  dashboardHost: controlplane.example.com
  backendHost: api.controlplane.example.com

backend:
  replicas: 2
  env:
    LOCAL_DOMAIN: stores.example.com
    HELM_CHART_PATH: /app/charts/woocommerce
    WORKER_MAX_CONCURRENCY: "4"
